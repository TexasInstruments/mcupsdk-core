%%{
    let common = system.getScript("/common");
    let module = system.modules['/drivers/gpadc/gpadc'];
%%}
/*
 * GPADC
 */
#include <drivers/gpadc.h>

/* GPADC Driver open/close - can be used by application when Driver_open() and
 * Driver_close() is not used directly and app wants to control the various driver
 * open/close sequences */
void Drivers_gpadcOpen(void);
void Drivers_gpadcClose(void);
